import {} from 'dotenv/config'

const config = {
     username_handle: process.env.USERNAME_HANDLE,
     email: process.env.EMAIL,
     password: process.env.X_PASSWORD,
     logged: true
}

export default config;